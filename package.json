{
  "name": "projectbu",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
